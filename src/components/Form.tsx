/**
 * TODO: Replace with @anilanar/form once it's ready.
 * @anilanar/form provides building functional & composable
 * blocks for common form features such as touched/dirty/invalid tracking
 * with controlled/uncontrolled forms.
 */

import styled from '@emotion/styled/macro';

export const Form = styled.form``;
